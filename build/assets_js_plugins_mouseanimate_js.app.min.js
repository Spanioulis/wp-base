(self["webpackChunkmaneko"] = self["webpackChunkmaneko"] || []).push([["assets_js_plugins_mouseanimate_js"],{

/***/ "./assets/js/plugins/mouseanimate.js":
/*!*******************************************!*\
  !*** ./assets/js/plugins/mouseanimate.js ***!
  \*******************************************/
/***/ (() => {

// ANIMATED DIV FOLLOWING CURSOR

(function mouseAnimate() {
  var triggers = $('.js-mouseanimate-trigger');
  console.log('mouseAnimate loaded', triggers);

  // create styles
  var style = document.createElement('style');
  style.textContent = ".js-mouseanimate {\n            position: absolute;\n            pointer-events: none;\n            opacity: 0;\n            z-index: 1;\n            transform: translateY(-50%);\n        }";
  document.head.append(style);

  // prepare each trigger
  if (triggers.length) {
    triggers.each(function (i) {
      var trigger = $(this);

      // we look for the animated element
      var animatedDiv = trigger.find('.js-mouseanimate');

      // if we don't find it, we look for a message in data-mouseanimate
      if (!animatedDiv[0]) {
        var message = trigger.attr('data-mouseanimate');
        animatedDiv = $('<div>', {
          id: 'animatedDiv-' + i,
          "class": 'js-mouseanimate-msg'
        });
        $(' body ').append(animatedDiv);
        setMessage(message, animatedDiv);
      }

      // we create all event listeners
      trigger.on('mouseenter', function () {
        showBox(animatedDiv);
        $(window).on('mousemove', function (e) {
          updateDisplay(e, animatedDiv);
        });
      });
      trigger.on('mouseleave', function () {
        hideBox(animatedDiv);
        $(window).off('mousemove');
      });
    });
  }
  function isTouchDevice() {
    return 'ontouchstart' in window || navigator.maxTouchPoints > 0 || navigator.msMaxTouchPoints > 0;
  }
  function setMessage(msg, div) {
    div.html(msg);
  }
  function updateDisplay(e, div) {
    div.css('left', e.pageX + 'px');
    div.css('top', e.pageY + 'px');
  }
  function showBox(div) {
    if (!isTouchDevice()) {
      div.css('opacity', '1');
    }
  }
  function hideBox(div) {
    div.css('opacity', '0');
  }
})();

/***/ })

}]);
//# sourceMappingURL=assets_js_plugins_mouseanimate_js.app.min.js.map